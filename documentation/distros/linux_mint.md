# Codecs

sudo nala install -y software-properties-common
sudo add-apt-repository multiverse  
sudo nala install -y libavcodec-extra mint-meta-codecs

# Microsoft Fonts

sudo nala install -y ttf-mscorefonts-installer

# Nala 

sudo apt install -y nala

# Software Manager

- Menu>Preferences
    - Enable unverified flatpaks
    
# Update GRUB

sudo update-grub
