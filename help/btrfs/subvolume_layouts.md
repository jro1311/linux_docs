# Subvolume Layout

- **single distro**
    - @ mount to /
    - @home mount to /home
    
- **multi distro**
    - @fedora mount to /
    - @home-fedora mount to /home
    - @flatpak mount to /var/lib/flatpak
